name: Agent P5 workspace export

on:
  push:
    branches:
      - boolean_symbolic
    paths:
      - '.github/workflows/agent-p5-workspace-export.yml'
  pull_request:
    paths:
      - '.github/workflows/agent-p5-workspace-export.yml'

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Create workspace archive
        run: tar --exclude='./.git' -czf /tmp/ygor-boolean-symbolic.tar.gz .
      - name: Upload workspace archive
        uses: actions/upload-artifact@v4
        with:
          name: ygor-boolean-symbolic-workspace
          path: /tmp/ygor-boolean-symbolic.tar.gz
          if-no-files-found: error
          retention-days: 1
