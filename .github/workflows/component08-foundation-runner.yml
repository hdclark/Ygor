name: Component 08 validated checkpoint publisher

on:
  workflow_run:
    workflows:
      - Component 07 workspace export
    types:
      - completed
  schedule:
    - cron: '*/5 * * * *'

permissions:
  contents: write

concurrency:
  group: component08-checkpoint-publisher
  cancel-in-progress: false

jobs:
  apply-validate-publish:
    if: github.event_name == 'schedule' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          repository: hdclark/Ygor
          fetch-depth: 0
          ref: agent/bounded-manifold-mesh-boolean-plan

      - name: Check pending checkpoint
        id: pending
        shell: bash
        run: |
          run=true
          for part in 00 01 02 03 04; do
            test -f ".github/component08-checkpoint.patch.part-${part}" || run=false
          done
          echo "run=${run}" >> "$GITHUB_OUTPUT"

      - name: Verify and apply exact checkpoint
        if: steps.pending.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cat .github/component08-checkpoint.patch.part-* \
            | base64 --decode | gzip --decompress \
            > /tmp/component08-checkpoint.patch
          echo "de2ddcf16114152cb09267338b1435210005af55d5df181393ee017ca4f7937a  /tmp/component08-checkpoint.patch" \
            | sha256sum --check --strict
          git apply --check /tmp/component08-checkpoint.patch
          git apply --index /tmp/component08-checkpoint.patch
          git diff --check --cached

      - name: Configure strict build
        if: steps.pending.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cmake -S . -B /tmp/ygor-component08-build \
            -DBUILD_TESTING=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -DMEMORY_CONSTRAINED_BUILD=ON \
            -DWITH_EIGEN=OFF \
            -DWITH_GNU_GSL=OFF

      - name: Build and run Component 08 qualification
        if: steps.pending.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          cmake --build /tmp/ygor-component08-build \
            --target \
              ygor_mesh_boolean_intersection_key_tests \
              ygor_mesh_boolean_intersection_interning_tests \
              ygor_mesh_boolean_intersection_preflight_tests \
              ygor_mesh_boolean_intersection_coordinate_tests \
            --parallel 2
          ctest --test-dir /tmp/ygor-component08-build \
            --output-on-failure \
            -R 'mesh_boolean_bounded_component08_(keys|interning|preflight|coordinates)'

      - name: Commit validated checkpoint to PR 90
        if: steps.pending.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/component08-checkpoint.patch.part-*
          rm -f .github/component08-foundation-source.patch.b64
          rm -f .github/component08-registry.patch.part-*
          rm -f .github/component08-events.patch.part-*
          rm -f .github/component08-binding-order.patch.part-*
          rm -f .github/workflows/component08-apply.yml
          git add .
          git diff --check --cached
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Implement Component 08 preflight, event interning, and coordinates (plan 08)'
          git push origin HEAD:agent/bounded-manifold-mesh-boolean-plan

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: component08-validated-checkpoint-failure
          path: |
            /tmp/component08-checkpoint.patch
            /tmp/ygor-component08-build/Testing/Temporary/LastTest.log
            /tmp/ygor-component08-build/CMakeFiles/CMakeOutput.log
          if-no-files-found: ignore
          retention-days: 3
