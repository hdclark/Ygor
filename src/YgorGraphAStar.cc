//YgorGraphAStar.cc - Generic A* graph/path finding helpers.

#include "YgorGraphAStar.h"

// The implementation is template-based and therefore lives in the header.
// This translation unit exists so build systems that expect a matching source
// file for each public header discover the module consistently.
