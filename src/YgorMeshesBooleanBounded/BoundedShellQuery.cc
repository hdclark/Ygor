// Component 09 bounded shell-query provider. The provider is header-only
// (BoundedShellQuery.h); this translation unit exists to guarantee the header
// compiles standalone under the strict floating-point profile.
#include "BoundedShellQuery.h"
