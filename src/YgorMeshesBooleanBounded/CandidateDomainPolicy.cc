#include "StrictFloatingBuild.h"
#include "CandidateDomainPolicy.h"

namespace ygor::mesh_boolean::bounded {
static_assert(broad_phase_leaf_capacity_v1 == 8,
              "Component 06 V1 leaf capacity is frozen");
} // namespace ygor::mesh_boolean::bounded
