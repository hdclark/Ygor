// Component 09 canonical codec. The codec is header-only
// (ClassificationCodec.h); this translation unit guarantees the header compiles
// standalone under the strict floating-point profile.
#include "ClassificationCodec.h"
