// Component 09 independent verifier. The verifier is header-only
// (ClassificationVerifier.h); this translation unit guarantees the header
// compiles standalone under the strict floating-point profile.
#include "ClassificationVerifier.h"
