#include "MeshBooleanGlobalArrangementFixtures.h"
#include <iostream>
using namespace arrangement_test;
int main(){try{
  auto r=arrangement_test::registry();auto a=cube<double,std::uint32_t>(),b=cube<double,std::uint32_t>();translate(b,3,0,0);auto c=context(a,b,r);auto result=build_global_arrangement(*c);if(!result.has_value())throw std::runtime_error(render_error(result.error()));const auto&x=*result.value()->payload;require(result.value()->report.passed(),"verified");structural_oracle(x);require(x.seams.empty()&&x.coincident_groups.empty(),"disjoint topology");require(c->artifacts().latest_generation(artifact_slot::arrangement_complex)==1,"published");auto repeated=build_global_arrangement(*c);require(repeated.has_value()&&repeated.value().get()==result.value().get(),"idempotent");
  const auto type=arrangement_complex_type_tag+(static_cast<std::uint64_t>(coordinate_tag::binary64)<<8);auto spec=r->specification(artifact_slot::arrangement_complex,type,arrangement_complex_schema,verification_level::mandatory).value();verification_environment_view env;env.owner=c->owner();env.setup_digest=c->replay().setup;env.coordinate=coordinate_tag::binary64;env.index=index_tag::uint32;env.exact_kernel=&c->kernel();env.accountant=&c->accountant();auto reject=[&](auto mutate){auto changed=std::make_shared<arrangement_complex<double,std::uint32_t>>(x);mutate(*changed);artifact_view view{c->owner(),artifact_slot::arrangement_complex,type,arrangement_complex_schema,1,x.artifact_digest,changed,changed.get()};auto checked=r->verify(view,spec,env);require(checked.has_value()&&!checked.value().passed(),"mutation rejected");};reject([](auto&changed){changed.halfedges.front().sheet_mate=changed.halfedges.front().id;});reject([](auto&changed){changed.probes.front().evidence.front()=exact_sign::zero;});reject([](auto&changed){changed.vertex_sectors.front().boundary_rays.front().antipode=0;});std::cout<<"ok\n";
}catch(const std::exception&e){std::cerr<<e.what()<<'\n';return 1;}}
