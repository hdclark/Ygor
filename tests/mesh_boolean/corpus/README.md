# Mesh Boolean corpus

`manifest.tsv` is ordered by stable record ID. Records bind a reviewed fixture
recipe to an exact semantic expectation; decimal geometry files are not used as
authoritative replay input. Regression records are added rather than replacing
historical records.
