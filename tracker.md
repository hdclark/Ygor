# Implementation tracker

- [x] Component 1 contracts, immutable IDs, diagnostics, and deterministic utilities.
- [x] Component 3 exact arithmetic and predicates, alongside Component 14 arithmetic/predicate oracles.
- [x] Component 13 artifact verifiers and Component 2 topology validation.
- [x] Component 4 broad phase, tested against exhaustive pair enumeration.
- [x] Components 5 and 6 symbolic events and registry.
- [x] Component 7 local arrangements, first on synthetic exact constraints and then on discovered events.
- [x] Component 8 global stitching and manifold/arrangement checks.
- [x] Component 9 classification, then Component 10 selection.
- [x] Component 11 checked realization and impossibility reporting.
- [x] Component 12 output assembly and canonicalization.
- [x] Full Component 14 adversarial, randomized, metamorphic, replay, and performance suites.
- [x] Convert every plan-gap case from `tests/MeshBooleanPlanGapCases.md` into an executable CI test with an explicit success or typed-failure expectation. These tests are expected to compile, but are not expected to pass until the broad plan and implementation components are updated.
- [x] Resolve the contract and algorithm gaps documented in `tests/MeshBooleanPlanGapCases.md`. The broad plan and Components 1, 3, 6, and 8-14 now distinguish exact geometry from topological occurrences, stratified selection from manifold publication, direct side classification from transition auditing, and exact-in-`T` semantics from future approximate output.
- [x] Components 1, 8, 9, and 13: implement topological occurrence IDs, complete spherical vertex links, certified open patch-side probes, frozen `independent_patch_side_v1` classification, independent arrangement reconstruction, and G3-G6/G9a/G9b/G9d gates.
- [x] Components 1, 10, 12, 13, and 14: implement stratified selected-boundary occurrences and topology classification, manifold-result preflight, `result_topology_not_supported`, and the complete G1 operation/type matrix.
- [x] Components 1, 3, 11, 13, and 14: implement `exact_in_T`, executable defining relations and exact substitution obligations, independent realization verification, and G2/G7/G9c gates.
- [x] Components 1, 11, 13, and 14: implement realization constraint-component decomposition, canonical component transcripts, conservative candidate-domain triangle broad phase, bounded exhaustive oracles, and G8 scalability gates.
- [x] Component 14: remove the plan-gap red-test label only after all G1-G9 tests pass in required Debug/Release, compiler, type, permutation, and replay matrices; retain the touching-cube and one-third fixtures permanently in the regression corpus.
- [x] Create a full but minimal example for end-users which assumes they have two `fv_surface_mesh<double, uint64_t>` meshes of unknown provenance, and a desired boolean operation (union, intersection, exclusion, subtraction). Focus on (1) using the new boolean system effectively, (2) validating the inputs as necessary, (3) properly handling and reporting errors, (4) extracting the resulting manifold mesh, when availble, into a `fv_surface_mesh<double, uint64_t>`, and (5) add the example as an end-to-end test to the test corpus. Ensure you provide descriptive inline comments as end-users will moslty be expected to learn how to use the new boolean engine via following this example!

## Performance acceleration workstream

Complete these items strictly in the order defined by `plan_speed.md`. Do not check an item until its focused tests, affected component tests, exact baseline comparison, and required broader qualification gate pass.

- [x] **P0 — Freeze exact and performance baselines.** Implement the versioned stage producer/verifier timing and deterministic counter facility from `plan_speed.md` sections 2-3; expand `benchmarks/MeshBooleanBenchmark.cc` to B0-B8; add `tests/Test_MeshesBooleanPerformanceBaselines.cc` and its Component 14 CTest registration; record exact typed outcomes, stage semantic digests, canonical output bytes, structural counters, and controlled Release benchmark results before changing algorithms. Run the full Component 1-14, Plan Gap, replay, mutation, qualification, and example suites.
- [x] **P1.1 — Remove full artifact copies.** Refactor `src/YgorMeshesBooleanTransaction.h` and every Component 2-12 producer so the transaction freezes, verifies, and publishes one allocation; remove `new Artifact(*candidate)`-style copies. Add synthetic large-artifact copy/move, failed-verification, stale-binding, rollback, prior-generation immutability, and resource-reservation tests. Confirm all P0 canonical bytes and digests remain unchanged.
- [x] **P1.2 — Remove encoding from hot comparisons.** Replace repeated `canonical_encoder` construction in event incidence/ownership comparison, construction interning, registry ordering, arrangement ordering, realization ordering, and output ordering with typed structural comparisons or once-computed immutable keys. Add generated equivalence tests against the previous canonical-byte comparator and deliberate digest-collision tests; retain full canonical encoding only at publication/verification boundaries.
- [x] **P1.3 — Add dense lookup indices and batched exact resource accounting.** Replace strong-ID/raw-ordinal `find_if` scans with immutable indexed tables, and batch reservations only after checked cardinality calculation. Add malformed handle, stale index, hash collision, and `limit-1`/`limit`/`limit+1` tests for every changed resource path. Run Components 1, 2, 5-13, mutation, and replay tests.
- [x] **P2.1 — Add exact-arithmetic microbenchmarks and differential oracles.** Extend Component 3 tests for limb-scale add/multiply/divide/GCD and rational operations, including dyadic, cancellation-heavy, quotient-correction, large-limb, and canonical serialization vectors. Keep the current simple division/GCD implementation as a bounded test oracle before replacing production algorithms.
- [x] **P2.2 — Optimize exact integer storage and division.** Add inline zero/one/two-limb `big_uint` storage, power-of-two and single-limb division fast paths, and normalized multi-limb long division; eliminate production bit-at-a-time division. Validate `a == q*b+r`, remainder bounds, exception safety, canonical bytes, allocation counts, and all `MeshExactArithmetic.Unit`/kernel property tests before running the full Boolean suite.
- [x] **P2.3 — Optimize GCD, rational normalization, and comparison.** Add deterministic limb-level GCD fast paths, cross-cancellation, reduced-result factories, dyadic comparison/alignment, and sign/bit-bound comparison shortcuts without weakening canonical rational form. Require unchanged exact-number serialization and P0 artifacts across B0-B8.
- [x] **P3.1 — Implement certified filters one predicate at a time.** In `src/YgorMeshesExactKernelFilters.cc`, add proof-versioned `orient2d`, then `orient3d`, then plane-side/dot-sign filters with strict uncertain-to-exact fallback. Add forced disabled/accept/fallback paths, permutation and degeneracy tests, signed-zero/subnormal/rounding/FTZ-DAZ tests, and filter-on/off artifact equivalence across all component tests.
- [x] **P3.2 — Remove exact divisions from incidence-only queries and add the dyadic support-plane path.** Split segment incidence from parameter construction, rewrite point-on-segment/polygon boundary tests with exact orientation and ranges, and construct input support planes by aligned dyadic integers with one primitive normalization. Differentially compare every fast path with the general exact implementation under Components 2, 3, 5, 7, 9, and 11 tests.
- [x] **P4.1 — Make face canonicalization and support-plane discovery linear.** Precompute directed and unoriented minimal ring rotations once, add raw-ordinal lookup maps, and replace the all-triples support-plane search with the deterministic first non-collinear triple scan. Compare against exhaustive rotation/triple oracles under ring, face, index, shell, and component permutations.
- [x] **P4.2 — Add conservative self broad phases and incremental triangulation.** Accelerate simple-ring edge pairs, same-operand embeddedness pairs, and `enumerate_bounded_feature_self()` with exact closed bounds and zero-false-negative exhaustive comparison. Replace repeated exhaustive ear searches with a deterministic linked-ring/reflex-candidate implementation while retaining exact visibility, area, and partition checks. Run Component 2 unit/property/invalid fuzz/re-ingestion tests.
- [ ] **P4.3 — Add verified immutable facet geometry reuse and scalable graph canonicalization.** Stop rebuilding projected rings, edge/triangle bounds, normals, and vertex fans in downstream stages. If P0 proves `canonicalize_graph_exhaustive()` dominant, implement independent color refinement plus bounded canonical individualization, retaining exhaustive small-graph comparison and full permutation invariance.
- [ ] **P5 — Optimize Component 4 producer and independent verifier broad phases.** Precompute axis keys, avoid sorting every BVH subtree, use deterministic private candidate buffers and k-way merge, and replace the verifier sweep's vector active set with an independently implemented interval index. Require exact candidate-byte equality and exhaustive no-false-negative checks for touching, one-ULP, subnormal, extreme, coincident-bound, insertion-order, and thread-count cases.
- [ ] **P6.1 — Index Component 5 event discovery.** Reuse P4 facet geometry, make facet-pair tasks private and canonically merged, precompute `(carrier, facet)` cuts, and remove repeated ring/provenance reconstruction. Run the complete event relation matrix, operand-swap, retriangulation/subdivision, exact-substitution, and low-complexity oracle tests.
- [ ] **P6.2 — Replace Component 6 quadratic/cubic identity work.** Use fingerprint buckets followed by exact equality for points/carriers/intervals; assign IDs only after exact equivalence classes are sorted. Rewrite the independent verifier with independently sorted exact classes and indexed lookup, retaining pairwise exhaustive small-case oracles. Test equal derivations, same-rounded-distinct points, hash collisions, permutations, and canonical serialization.
- [ ] **P7 — Add per-facet constraint indices and a conservative local 2D broad phase.** Replace `audit_crossings()` full facet/curve/raw-interval scans with precomputed incidence tables and exact-point membership; enumerate possible segment crossings by exact closed bounds; use dense deterministic DCEL tables; prove reconciliation generations make progress. Compare every bounded case with exhaustive crossing/reconciliation oracles and run all Component 7, Plan Gap, and coverage tests.
- [ ] **P8.1 — Localize global stitching and spherical links.** Build sorted flat incidence groups and dense maps for source sheets, seams, coincident groups, occurrences, patch sides, and local link germs; use conservative local arc candidates while retaining exhaustive independent link verification. Run Component 8, standalone verifier-link, G3-G6/G9, mutation, subdivision, input-order, and scheduling tests.
- [ ] **P8.2 — Optimize the complete exact patch witness without reintroducing heuristics.** Cache projected outer/hole rings and slab edge events in `patch_interior()`; preserve exact vertical decomposition and exact lift. Never restore centroid or finite heuristic probes. Keep the through-column annulus and add multi-hole, thin-corridor, vertical-edge, repeated-x, and ring-permutation regressions; require unchanged valid witness/canonical probe evidence or an explicitly versioned formula change.
- [ ] **P9.1 — Remove per-triangle source-ring/fan duplication.** Build one immutable source-facet classification record and lightweight sourced triangle references independently in producer and verifier. Add memory/resource tests proving ring and vertex-fan storage scales per facet rather than per triangle.
- [ ] **P9.2 — Add independent conservative point-location acceleration.** Give the producer a triangle/facet ray hierarchy and the verifier a different facet-level sweep/hierarchy; uncertain/touching formal-ray bounds remain candidates. Compare complete hit records, ownership, grouping, signed degree, shell polarity, and primary/alternate rays with exhaustive scans on every Component 9 fixture. Add a conservative global-patch index for exterior attachment and retain exact full-scan differential tests.
- [ ] **P10 — Add the exact-in-`T` singleton realization path and sparse obligation/output handling.** Check exact representability once per selected axis, bypass generic DFS only when every domain is singleton, but still generate and independently verify the complete obligation universe, component graph, pair candidates, and canonical transcript. Precompute realization sort keys, keep exhaustive triangle-pair oracle tests, move verified arrays into output assembly without copies, and run Components 11-12, G2/G7/G8/G9c, impossible-coordinate, mutation, and re-ingestion tests.
- [ ] **P11 — Audit and bound all remaining mandatory verifier costs.** For every verifier still performing repeated full scans or accidental all-pairs work, add an independent sorted/sweep/hierarchy reconstruction and keep the prior exhaustive path as a bounded Component 14 oracle. Do not remove mandatory invariants or violate standalone verifier-link separation. Run all mutation, self-consistent mutation, stale-evidence, verifier resource-limit, replay, and exhaustive-vs-mandatory differential tests.
- [ ] **P12 — Replace per-task `std::async` with deterministic bounded workers and parallelize only independent units.** Use canonical task keys, private result slots, canonical merge, canonical first-error selection, complete cancellation/join/rollback, and enforced `max_queued_tasks`. Require byte-identical artifacts, reports, errors, replays, and deterministic counters for thread counts 1, 2, and maximum across event, refinement, link, classification, and realization-component workloads.
- [ ] **P13 — Complete qualification and enforce frozen performance gates.** Run the Debug/Release, GCC/Clang, supported type, mandatory/exhaustive, filter-off/on, thread-count, sanitizer, debug-library, permutation, replay, fuzz, mutation, Plan Gap, example, and qualification matrix in `plan_speed.md` section 16. On controlled Release runners, meet the frozen P0 targets (initially 4x geometric mean on B1-B7, 5x on B6, no unexplained >15% regression, no authoritative-byte increase), and document before/after producer/verifier timing, deterministic counters, peak bytes, tests, versions, and safety argument for every completed package.
