# Boolean4 Repair Tracker

- [x] Add failing regression tests for current Boolean4 behavior. Verified by compiling `tests/Test_MeshesBoolean4.cc` and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; it fails on the known `A - empty` algebra bug and nondeterministic disjoint-cube output.
- [x] Fix immediate `BooleanMeshOp4` contract bugs and enum validation. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and confirming the empty-operand algebra and wrapper diagnostic sections pass; the remaining disjoint-cube nondeterminism failure is a later-plan BSP replacement issue.
- [x] Define the internal exact/topology-first Boolean4 data model. Verified by rebuilding `ygor` after adding private stable-id records for operand/source references, snap-rounded topology coordinates, constructed vertices, halfedges, fragments, components, and shells.
- [x] Normalize and validate inputs explicitly with operand-specific diagnostics. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and confirming empty-operand algebra plus wrapper/operand diagnostic tests pass before the remaining later-plan nondeterminism failure.
- [x] Build a deterministic triangle-pair broad phase. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new deterministic broad-phase section passes for coplanar overlap plus edge/vertex-touching AABBs, while the remaining closed-mesh nondeterminism failure is the later-plan BSP replacement issue.
- [x] Implement robust triangle-triangle classification and exact construction records. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new triangle-triangle classification section passes for disjoint, proper segment, point contact, shared/overlapping edge, coplanar disjoint, coplanar area overlap, identical, and reversed-identical cases. The remaining closed-mesh nondeterminism failure is the later-plan BSP replacement issue.
- [x] Split every input triangle into arrangement facets. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new arrangement facet splitting section passes for unconstrained triangles, deterministic constraint splitting, area preservation, and preserved constraint edges. The remaining closed-mesh nondeterminism failure is the later-plan BSP replacement issue.
- [x] Classify arrangement facets against the opposite solid. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new arrangement facet classification section passes for outside, inside, on-boundary same-facing, on-boundary opposite-facing, and deterministic per-facet emission. The remaining closed-mesh nondeterminism failure is the later-plan BSP replacement issue.
- [x] Select output facets with direct Boolean truth tables. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new boolean facet selection truth-table section passes for union, intersection, subtraction, exclusion, same-facing coincident facets, opposite-facing contact facets, orientation reversal, reason codes, and deterministic decisions. The remaining closed-mesh nondeterminism failure is the later-plan BSP replacement issue.
- [x] Assemble the output through a halfedge mesh using exact topology ids. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new output halfedge assembly section passes for topology-key vertex merging, deterministic cube emission, orientation preservation, and non-manifold edge rejection. The remaining disjoint-cube nondeterminism failure is the later-plan BSP replacement issue.
- [x] Orient output facets by construction and enforce postconditions. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new output postcondition verification section passes for valid output, duplicate-face rejection, and operation-named invariant diagnostics. The remaining disjoint-cube nondeterminism failure is the later-plan BSP replacement issue.
- [x] Remove nondeterministic and approximate BSP dependencies from Boolean4. Verified by rebuilding `ygor`, confirming `src/YgorMeshesBoolean4.cc` has no remaining `YgorMeshesBSPTree`, `bsp_tree`, or `BSP` references, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the earlier disjoint-cube nondeterminism no longer fails. The remaining closed-mesh failure is now `overlapping cubes union: Boolean4 output assembly produced a non-manifold edge between vertices 1 and 2 with 1 incident faces`, which is a later arrangement/topology completion issue.
- [x] Handle empty, identity, and unexpected universal cases correctly. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new empty-operand algebra and identity-normalization sections pass for vertex-only empty meshes, normalized identity outputs, and non-finite vertex-only rejection. The remaining closed-mesh failure is still `overlapping cubes union: Boolean4 output assembly produced a non-manifold edge between vertices 1 and 2 with 1 incident faces`, which is a later arrangement/topology completion issue.
- [x] Add diagnostics and optional deterministic debug dumps. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new diagnostics/debug-dump section passes for validation, triangle-pair predicate, arrangement split, facet classification, output assembly diagnostics, deterministic dump output, source ids, and decision reason names. The remaining closed-mesh failure is still `overlapping cubes union: Boolean4 output assembly produced a non-manifold edge between vertices 1 and 2 with 1 incident faces`, which is a later arrangement/topology completion issue.
- [x] Add property, metamorphic, and deterministic fuzz tests. Verified by recompiling `tests/Test_MeshesBoolean4.cc` and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new property/deterministic fuzz identity and metamorphic transform sections pass for fixed-seed convex cube cases, Boolean identities, and translation/rotation/scale metamorphic checks. The remaining closed-mesh failure is still `overlapping cubes union: Boolean4 output assembly produced a non-manifold edge between vertices 1 and 2 with 1 incident faces`, which is a later arrangement/topology completion issue.
- [x] Validate numerical robustness for all explicit template instantiations. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/ygor-build/Test_MeshesBoolean4`; the new numerical robustness boundaries section passes for deterministic identity outputs across `float`/`double` and `uint32_t`/`uint64_t`, plus explicit index overflow guards. The remaining closed-mesh failure is still `overlapping cubes union: Boolean4 output assembly produced a non-manifold edge between vertices 1 and 2 with 1 incident faces`, which is a later arrangement/topology completion issue.
- [x] Replace `BooleanMeshOp4` with the arrangement pipeline. Verified by rebuilding `ygor`, compiling `tests/Test_MeshesBoolean4.cc` against the build-tree library, and running `/tmp/opencode/Test_MeshesBoolean4`; the full Boolean4 regression suite now passes, including closed mesh operation cases.
- [x] Update Boolean4 public comments and algorithm documentation. Verified by rebuilding `ygor`, recompiling `tests/Test_MeshesBoolean4.cc` against the build-tree library, and running `/tmp/opencode/Test_MeshesBoolean4`; the full Boolean4 regression suite passes after replacing stale BSP-backed documentation with arrangement-pipeline comments, supported-input rules, output invariants, and deterministic topology-key policy.
- [x] Run full build, regression tests, fuzz tests, and sanitizers. Verified by configuring and building `/tmp/opencode/ygor-build`, compiling and running `/tmp/opencode/Test_MeshesBoolean4` with all Boolean4 regression/property/fuzz sections passing, running all executable `tests/test_*` binaries successfully, configuring and building `/tmp/opencode/ygor-asan-build -DWITH_ASAN=ON`, and compiling/running `/tmp/opencode/Test_MeshesBoolean4_asan` with all Boolean4 regression/property/fuzz sections passing under ASan/UBSan. The legacy `tests2/run_tests` binary was attempted but is stale in this checkout and fails before tests with missing symbol `ygor::io::gzip_ostream::gzip_ostream(std::ostream&)`.
- [x] Remove or quarantine obsolete BSP-based assumptions from Boolean4. Verified by confirming `src/YgorMeshesBoolean4.cc` contains no `YgorMeshesBSPTree`, `bsp_tree`, or `BSP` references, adding a Boolean4 regression guard that fails if those tokens return to the implementation, recompiling `tests/Test_MeshesBoolean4.cc`, and running `/tmp/opencode/Test_MeshesBoolean4` with all sections passing including `no BSP dependency guard`.
